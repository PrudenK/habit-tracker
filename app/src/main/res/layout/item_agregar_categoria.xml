<Button android:id="@+id/addButton"
    android:layout_width="@dimen/w_item_agregar_categoria"
    android:layout_height="@dimen/h_item_agregar_categoria"
    android:text="+"
    android:textSize="@dimen/t_item_agregar_categoria"
    android:backgroundTint="@color/boton_agregar_categoria"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:textColor="@color/tittle_color"
    />
